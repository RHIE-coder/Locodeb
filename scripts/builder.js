const optimismNode = require("../nodes/optimism");

const commands = {
    optimism: {
        ['--mode']: {
            alias: '-m',
            desc: 'select the mode to build a network',
            allows: ['local'],
        },

    } 
}


(async()=>{

})()